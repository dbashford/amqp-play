Fanout exchange (TEST.fanout) with two queues (a-fanout-queue-1, a-fanout-queue-2).  Web client to publish to exchange.  Can also publish to exchange via Rabbit web client

In two terminal windows:
1) coffee amqp_server.coffee
2) coffee web_server.coffee

When messages are published to the exchange via either mechanism you should see those messages pop up on the amqp_server console.

