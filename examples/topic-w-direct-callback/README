Queue bound to topic exchange receiving published messages.  Queue consumer reverses string and puts response onto direct exchange routed to the replyBy.  Result is then ajax pushed to client via now.

In two terminal windows:
1) coffee amqp_server.coffee
2) coffee web_server.coffee

Then open http://localhost:8080 to view test client page

